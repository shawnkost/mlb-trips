services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: mlb
      POSTGRES_PASSWORD: mlb
      POSTGRES_DB: mlb_trips
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgressql/data
volumes:
  postgres_data:
